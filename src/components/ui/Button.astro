---
type ButtonVariant = 'primary' | 'secondary' | 'ghost';
type ButtonSize = 'sm' | 'md' | 'lg';

interface Props {
  variant?: ButtonVariant;
  size?: ButtonSize;
  href?: string;
  text: string;
  external?: boolean;
  onClick?: string;
  class?: string;
}

const {
  variant = 'primary',
  size = 'md',
  href,
  text,
  external = false,
  onClick,
  class: customClass = '',
} = Astro.props;

const variantStyles: Record<ButtonVariant, string> = {
  primary: 'bg-primary text-white hover:bg-opacity-90',
  secondary: 'bg-secondary text-white hover:bg-opacity-90',
  ghost: 'text-primary border border-primary hover:bg-accent-light',
};

const sizeStyles: Record<ButtonSize, string> = {
  sm: 'px-4 py-2 text-sm',
  md: 'px-6 py-3 text-base',
  lg: 'px-8 py-4 text-lg',
};

const buttonClass = `inline-block rounded-lg font-semibold transition ${variantStyles[variant]} ${sizeStyles[size]} ${customClass}`;

const Tag = href ? 'a' : 'button';
---

<Tag
  href={href}
  target={external ? '_blank' : undefined}
  rel={external ? 'noopener noreferrer' : undefined}
  onclick={onClick}
  class={buttonClass}
>
  {text}
</Tag>

<style>
  a,
  button {
    cursor: pointer;
    display: inline-block;
    text-decoration: none;
    border: none;
    font-family: inherit;
  }

  button:focus,
  a:focus {
    outline: 2px solid var(--color-primary);
    outline-offset: 2px;
  }

  @media (hover: hover) {
    button, a, [data-btn] {
      transition: transform 0.2s ease, box-shadow 0.2s ease;
    }
    button:hover, a:hover, [data-btn]:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 24px rgba(0, 191, 216, 0.25);
    }
    button:active, a:active, [data-btn]:active {
      transform: translateY(0);
    }
  }
</style>
