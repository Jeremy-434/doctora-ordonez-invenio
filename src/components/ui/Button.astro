---
type ButtonVariant = 'primary' | 'secondary' | 'ghost';
type ButtonSize = 'sm' | 'md' | 'lg';

interface Props {
  variant?: ButtonVariant;
  size?: ButtonSize;
  href?: string;
  text: string;
  external?: boolean;
  onClick?: string;
  class?: string;
}

const {
  variant = 'primary',
  size = 'md',
  href,
  text,
  external = false,
  onClick,
  class: customClass = '',
} = Astro.props;

const variantStyles: Record<ButtonVariant, string> = {
  primary: 'bg-primary text-bg hover:bg-bg hover:text-primary border border-primary',
  secondary: 'bg-primary text-bg hover:bg-bg hover:text-primary border border-primary',
  ghost: 'text-primary border border-primary hover:bg-primary hover:text-bg',
};

const sizeStyles: Record<ButtonSize, string> = {
  sm: 'px-6 py-2 text-xs',
  md: 'px-8 py-3 text-xs',
  lg: 'px-10 py-4 text-sm',
};

const buttonClass = `inline-block rounded-pill font-medium uppercase tracking-widest transition-colors duration-300 ${variantStyles[variant]} ${sizeStyles[size]} ${customClass}`;

const Tag = href ? 'a' : 'button';
---

<Tag
  href={href}
  target={external ? '_blank' : undefined}
  rel={external ? 'noopener noreferrer' : undefined}
  onclick={onClick}
  class={buttonClass}
>
  {text}
</Tag>

<style>
  a,
  button {
    cursor: pointer;
    display: inline-block;
    text-decoration: none;
    font-family: inherit;
  }

  button:focus,
  a:focus {
    outline: 2px solid var(--color-border);
    outline-offset: 2px;
  }
</style>
