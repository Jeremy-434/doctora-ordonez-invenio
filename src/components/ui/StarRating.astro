---
interface Props {
  count: number;
}

const { count } = Astro.props;
---

<div class="flex gap-1">
  {
    Array.from({ length: 5 }).map((_, i) => (
      <span class={i < count ? 'text-gold text-lg' : 'text-text-faint text-lg'}>★</span>
    ))
  }
</div>
